openapi: 3.1.1
info:
  title: Tokyo Land Price RAG API
  version: "1.0.0"
paths:
  /health:
    $ref: ./paths/health.yml
components:
  schemas:
    ErrorResponse:
      $ref: ./components/schemas/error-response.yml
